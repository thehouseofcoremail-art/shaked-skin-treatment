# data/

| file | origin | committed |
|---|---|---|
| `schedule.json` | Hand-transcribed from the לוז's `שיבוץ … (לאסנה)` table. The authoritative week→unit mapping. | yes |
| `flags.json` | Editorial conflicts found while reading the לוז (`blocker` / `conflict` / `info`). | yes |
| `units.json` | Generated — `lib/extract-units.mjs` (step 2). Contains the client's full copy. | no |
| `existing-asana.json` | Snapshot — `mcp__Asana__search_tasks` (step 4). Stale on arrival. | no |

Regenerate both ignored files by following SKILL.md steps 1–2 and 4 before planning.
