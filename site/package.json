{
  "name": "behind-the-mirrors",
  "version": "1.0.0",
  "private": true,
  "description": "מאחורי המראות — מגזין מנויים על הצד העסקי של תעשיית הכושר בישראל",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:preview": "node build.mjs --demo",
    "test": "node test/auth.test.mjs",
    "dev": "npx wrangler pages dev public --compatibility-date=2026-01-01",
    "deploy": "npm run build && npx wrangler pages deploy public",
    "test:ui": "node build.mjs --demo && node test/ui.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
